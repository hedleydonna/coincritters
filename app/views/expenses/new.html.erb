<div class="max-w-2xl mx-auto py-8 px-4">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">Add One-Off Expense</h1>
    <%= link_to "← Back to Expenses", expenses_path, class: "text-gray-600 hover:text-gray-900 font-medium" %>
  </div>

  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-8">
    <%= form_with model: @expense, url: expenses_path, class: "space-y-6" do |f| %>
      <% if @expense.errors.any? %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
          <h3 class="text-red-800 font-semibold mb-2">
            <%= pluralize(@expense.errors.count, "error") %> prevented this expense from being saved:
          </h3>
          <ul class="list-disc list-inside text-red-700 space-y-1">
            <% @expense.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= f.label :expense_template_id, "Template (optional)", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.collection_select :expense_template_id, @expense_templates, :id, :name, 
              { prompt: "Select a template (optional)" }, 
              { class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" } %>
        <p class="mt-1 text-sm text-gray-500">Optionally base this expense on an existing template</p>
      </div>

      <div>
        <%= f.label :name, "Expense Name", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_field :name, 
              placeholder: "e.g. Birthday gift, Car repair, etc.",
              class: "w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
        <p class="mt-1 text-sm text-gray-500">Required if not using a template</p>
      </div>

      <div>
        <%= f.label :allotted_amount, "Amount", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <div class="relative">
          <span class="absolute left-3 top-2 text-gray-500">$</span>
          <%= f.number_field :allotted_amount, 
                step: "0.01", 
                min: "0",
                placeholder: "0.00",
                class: "w-full px-4 py-2 pl-8 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" %>
        </div>
      </div>

      <div class="flex gap-4 pt-4">
        <%= f.submit "Add Expense", 
              class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold px-6 py-3 rounded-lg transition-colors cursor-pointer" %>
        <%= link_to "Cancel", expenses_path, 
              class: "bg-gray-200 hover:bg-gray-300 text-gray-700 font-semibold px-6 py-3 rounded-lg transition-colors" %>
      </div>
    <% end %>
  </div>
</div>

