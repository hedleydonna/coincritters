<div class="min-h-screen bg-gradient-to-br from-purple-600 to-pink-600 py-12 px-4">
  <div class="max-w-4xl mx-auto">
    <div class="mb-8 flex justify-between items-center">
      <h1 class="text-5xl font-bold text-white mb-2">Income Details</h1>
      <%= link_to "← Back to Incomes", admin_incomes_path, class: "text-white hover:text-purple-200 font-medium" %>
    </div>

    <div class="bg-white rounded-2xl shadow-2xl p-8">
      <div class="space-y-6">
        <div>
          <label class="block text-sm font-medium text-gray-500">User</label>
          <p class="mt-1 text-lg text-gray-900">
            <%= @income.user.email %>
            <% if @income.user.display_name.present? %>
              <span class="text-gray-500">(<%= @income.user.display_name %>)</span>
            <% end %>
          </p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Income Name</label>
          <p class="mt-1 text-lg font-semibold text-gray-900"><%= @income.name %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Frequency</label>
          <p class="mt-1">
            <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
              <%= @income.frequency.humanize %>
            </span>
          </p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Estimated Amount</label>
          <p class="mt-1 text-2xl font-bold text-green-600">$<%= number_with_delimiter(@income.estimated_amount, delimiter: ",") %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Status</label>
          <p class="mt-1">
            <% if @income.active? %>
              <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-green-100 text-green-800 border border-green-200">Active</span>
            <% else %>
              <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-gray-100 text-gray-800 border border-gray-200">Inactive</span>
            <% end %>
          </p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Auto-Create</label>
          <p class="mt-1">
            <% if @income.auto_create? && @income.auto_day_of_month.present? %>
              <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-blue-100 text-blue-800 border border-blue-200">
                Enabled (Day <%= @income.auto_day_of_month %> of month)
              </span>
              <p class="mt-2 text-sm text-gray-600">
                Income events will be automatically created on the <%= @income.auto_day_of_month.ordinalize %> of each month.
                <br>
                <strong>Estimated amount ($<%= number_with_delimiter(@income.estimated_amount, delimiter: ",") %>) will be used as the actual amount</strong> in auto-generated events.
              </p>
            <% elsif @income.auto_create? %>
              <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-yellow-100 text-yellow-800 border border-yellow-200">
                Enabled (Day not set)
              </span>
              <p class="mt-2 text-sm text-yellow-600">Auto-create is enabled but day of month is not configured. Please edit to set a day.</p>
            <% else %>
              <span class="px-3 py-1 inline-flex text-sm leading-5 font-semibold rounded-full bg-gray-100 text-gray-500 border border-gray-200">Manual</span>
              <p class="mt-2 text-sm text-gray-600">
                User must manually create income events and enter the actual amount received (which may differ from the estimated amount above).
              </p>
            <% end %>
          </p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Created</label>
          <p class="mt-1 text-lg text-gray-900"><%= @income.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
        </div>

        <div>
          <label class="block text-sm font-medium text-gray-500">Last Updated</label>
          <p class="mt-1 text-lg text-gray-900"><%= @income.updated_at.strftime("%B %d, %Y at %I:%M %p") %></p>
        </div>
      </div>

      <div class="mt-8 flex gap-4">
        <%= link_to "Edit Income", edit_admin_income_path(@income), class: "bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg hover:shadow-xl" %>
        <%= button_to "Delete Income", admin_income_path(@income), method: :delete,
            data: { confirm: "Are you sure you want to delete this income? This action cannot be undone.", turbo_confirm: "Are you sure you want to delete this income? This action cannot be undone." },
            form: { data: { turbo_confirm: "Are you sure you want to delete this income? This action cannot be undone." } },
            class: "bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg hover:shadow-xl" %>
      </div>
    </div>
  </div>
</div>

