<div class="min-h-screen bg-gradient-to-br from-purple-600 to-pink-600 py-12 px-4">
  <div class="max-w-7xl mx-auto">
    <div class="mb-8 flex justify-between items-center">
      <div>
        <h1 class="text-5xl font-bold text-white mb-2">Manage Spending Categories</h1>
        <p class="text-purple-100 text-lg">View and manage all spending categories</p>
      </div>
      <%= link_to "+ New Category", new_admin_spending_category_path, class: "bg-white text-purple-600 hover:bg-purple-50 font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg hover:shadow-xl" %>
      <%= link_to "← Back to Admin Dashboard", admin_root_path, class: "text-white hover:text-purple-200 font-medium ml-4" %>
    </div>

    <!-- Stats Cards -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      <div class="bg-white rounded-2xl shadow-2xl p-8 transform hover:scale-105 transition duration-200">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide">Total Categories</h3>
            <p class="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-purple-600 to-pink-600 mt-2"><%= @total_categories %></p>
          </div>
          <div class="text-4xl">📁</div>
        </div>
      </div>
      <div class="bg-white rounded-2xl shadow-2xl p-8 transform hover:scale-105 transition duration-200">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide">Fixed</h3>
            <p class="text-4xl font-bold text-blue-600 mt-2"><%= @fixed_categories %></p>
          </div>
          <div class="text-4xl">🔒</div>
        </div>
      </div>
      <div class="bg-white rounded-2xl shadow-2xl p-8 transform hover:scale-105 transition duration-200">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide">Variable</h3>
            <p class="text-4xl font-bold text-green-600 mt-2"><%= @variable_categories %></p>
          </div>
          <div class="text-4xl">📊</div>
        </div>
      </div>
      <div class="bg-white rounded-2xl shadow-2xl p-8 transform hover:scale-105 transition duration-200">
        <div class="flex items-center justify-between">
          <div>
            <h3 class="text-sm font-medium text-gray-500 uppercase tracking-wide">Savings</h3>
            <p class="text-4xl font-bold text-pink-600 mt-2"><%= @savings_categories %></p>
          </div>
          <div class="text-4xl">💰</div>
        </div>
      </div>
    </div>

    <div class="bg-white rounded-2xl shadow-2xl p-8">
      <% if @spending_categories.any? %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gradient-to-r from-purple-50 to-pink-50">
              <tr>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">User</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Name</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Type</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Savings</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Default Amount</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Auto-Create</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Created</th>
                <th class="px-6 py-4 text-left text-xs font-bold text-gray-700 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="bg-white divide-y divide-gray-100">
              <% @spending_categories.each do |category| %>
                <tr class="hover:bg-purple-50 transition duration-150">
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                    <%= category.user.email %>
                    <% if category.user.display_name.present? %>
                      <br><span class="text-xs text-gray-500"><%= category.user.display_name %></span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900 font-semibold"><%= category.name %></td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <% if category.fixed? %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800 border border-blue-200">Fixed</span>
                    <% else %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800 border border-green-200">Variable</span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <% if category.is_savings? %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-pink-100 text-pink-800 border border-pink-200">Yes</span>
                    <% else %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-500 border border-gray-200">No</span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-bold text-gray-900">
                    <% if category.default_amount.present? %>
                      $<%= number_with_delimiter(category.default_amount, delimiter: ",") %>
                    <% else %>
                      <span class="text-gray-400">—</span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap">
                    <% if category.auto_create? %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800 border border-blue-200">Yes</span>
                    <% else %>
                      <span class="px-3 py-1 inline-flex text-xs leading-5 font-semibold rounded-full bg-gray-100 text-gray-500 border border-gray-200">No</span>
                    <% end %>
                  </td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-600"><%= category.created_at.strftime("%b %d, %Y") %></td>
                  <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                    <%= link_to "View", admin_spending_category_path(category), class: "text-purple-600 hover:text-purple-800 font-semibold mr-4 transition duration-150" %>
                    <%= link_to "Edit", edit_admin_spending_category_path(category), class: "text-pink-600 hover:text-pink-800 font-semibold mr-4 transition duration-150" %>
                    <%= button_to "Delete", admin_spending_category_path(category), method: :delete,
                        data: { confirm: "Are you sure you want to delete this category? All associated envelopes will be deleted.", turbo_confirm: "Are you sure you want to delete this category? All associated envelopes will be deleted." },
                        form: { style: "display: inline-block; margin: 0;", data: { turbo_confirm: "Are you sure you want to delete this category? All associated envelopes will be deleted." } },
                        class: "text-red-600 hover:text-red-800 font-semibold bg-transparent border-0 p-0 cursor-pointer transition duration-150" %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <div class="text-center py-12">
          <p class="text-gray-500 text-lg mb-4">No spending categories found.</p>
          <%= link_to "Create First Category", new_admin_spending_category_path, class: "bg-purple-600 hover:bg-purple-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200 shadow-lg hover:shadow-xl" %>
        </div>
      <% end %>
    </div>
  </div>
</div>

