<div class="w-full max-w-4xl mx-auto">
  <div class="mb-6">
    <%= link_to "← Back to Branches", expense_templates_path, 
          class: "text-gray-600 hover:text-gray-900 mb-4 inline-block" %>
    <h1 class="text-3xl font-bold text-gray-900">New Branch</h1>
    <p class="text-gray-600 mt-2">Create a branch that will be used to generate expenses in your monthly budgets</p>
  </div>
  <%= render "form" %>
</div>

