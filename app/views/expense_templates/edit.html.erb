<div class="w-full max-w-4xl mx-auto">
  <div class="mb-6">
    <%= link_to "← Back to Spending", expense_templates_path(return_to: @return_to), 
          class: "text-gray-600 hover:text-gray-900 mb-4 inline-block" %>
    <h1 class="text-3xl font-bold text-gray-900">Edit Spending Item</h1>
    <p class="text-gray-600 mt-2">Update your spending item settings</p>
  </div>
  <%= render "form" %>
</div>

