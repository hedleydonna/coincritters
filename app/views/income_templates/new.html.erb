<div class="w-full max-w-4xl mx-auto">
  <div class="mb-6">
    <%= link_to "← Back to Money In", income_templates_path(return_to: @return_to), 
          class: "text-gray-600 hover:text-gray-900 mb-4 inline-block" %>
    <h1 class="text-3xl font-bold text-gray-900">New Money In Source</h1>
    <p class="text-gray-600 mt-2">Create a money in source that will be used to generate income events in your monthly budgets</p>
  </div>
  <%= render "form" %>
</div>

